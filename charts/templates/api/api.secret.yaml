{{ if .Values.api.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helpers.fullname" . }}-secret
data:
  {{- range $key, $val := .Values.api.secret }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{ end }}